sudo: required
services:
    - docker

script:
    - docker build -t fred/node2test .
    - docker images fred/node2test

deploy:
    provider: script
    script: bash docker_push
    on:
        branch: main
    